import * as React from 'react';

interface IMasterPageProps {
}

const MasterPage: React.FunctionComponent<IMasterPageProps> = (props) => {
  return <div className=""></div>;
};

export default MasterPage;
