
import * as React from 'react';

interface IRightSideMasterProps {
}

const LeftSideMaster: React.FunctionComponent<IRightSideMasterProps> = (props) => {
  return <div className=""></div>;
};

export default LeftSideMaster;
